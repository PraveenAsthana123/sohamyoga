[general]
static=yes
writeprotect=yes

[from-local]
; Safe local validation only. No carrier trunk or PSTN route is defined.
exten => 600,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Playback(demo-echodone)
 same => n,Hangup()
exten => 700,1,Answer()
 same => n,Playback(hello-world)
 same => n,Hangup()

[from-carrier]
; Intentionally fail closed until a verified DID routing rule is deployed.
exten => _X.,1,NoOp(Unconfigured inbound DID ${EXTEN})
 same => n,Hangup(21)

