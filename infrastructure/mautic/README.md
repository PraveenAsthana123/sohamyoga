# Mautic campaign management

Self-hosted Mautic 6 with isolated MySQL, web, cron and worker roles, bound to `http://127.0.0.1:18090`.

Run `./scripts/setup-mautic-campaign-manager.sh`. Configure real mail transport, sender-domain authentication, consent and suppression rules before sending.
