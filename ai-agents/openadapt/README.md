# OpenAdapt · Desktop+browser process recorder

MIT · capture human workflows for agent training.

| File | Purpose |
|---|---|
| `deep/docs/setup.md` | Recording · visualization · replay |
| `deep/examples/` | Sample recorded workflows |
| `deep/scripts/install.sh` | pip install |
| `deep/backend/` | Replay manager integration |

## Install
`pip install openadapt` (or use `./scripts/setup_ai_agent_stack.sh --tool openadapt`)
