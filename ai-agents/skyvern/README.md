# Skyvern OSS · Production RPA browser agent

AGPL-3.0 · enterprise-grade vision-first RPA with LLM fallback.

| File | Purpose |
|---|---|
| `deep/docs/setup.md` | Docker compose · environment vars |
| `deep/examples/` | Task definitions (JSON schema) |
| `deep/scripts/install.sh` | Clone + docker compose up |
| `deep/backend/` | HTTP client to localhost:8000/api/v1/tasks |

## Install
`./scripts/setup_ai_agent_stack.sh --tool skyvern`
