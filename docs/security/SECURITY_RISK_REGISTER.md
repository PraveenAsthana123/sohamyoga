# Security Risk Register — Phase 7 Audit

Every row backed by a real command/grep/file-read, cross-referenced to
[SECURITY_ARCHITECTURE.md](SECURITY_ARCHITECTURE.md), [THREAT_MODEL.md](THREAT_MODEL.md), and
[SECURITY_GATES.md](SECURITY_GATES.md). Verified 2026-09-08. Severity is Critical/High/Medium/Low,
scored on realistic exploitability for this workspace (mostly single-operator, local/tunnel-exposed
apps), not a generic worst-case.

| ID | Category | Finding | Severity | Portal(s) | Likelihood | Evidence | Recommended remediation | Status |
|---|---|---|---|---|---|---|---|---|
| SEC-01 | Secrets management | OpenBao vault runs in `-dev` mode (in-memory, auto-unsealed, single root token) — any stored secret is lost on every container restart, no audit log, no real ACLs | **High** | ai-orchestrator-platform | Already materialized once (during this audit's own P0 fix) | `docker inspect` shows `Cmd: [server -dev]`; live `bao kv list secret/` returned empty after restart | Switch to a persistent storage backend (file or Raft) with real unseal/init, or accept the risk explicitly and document it — not both without a decision recorded | Open |
| SEC-02 | DevSecOps / CI gates | No portal's CI pipeline runs tests, SAST, SCA, or secrets scanning — only sohamyoga-frontend has CI at all, and it's lint/typecheck/build only | **High** | All 6 | Continuous (every push) | `.github/workflows/ci.yml` content; `find` confirms no CI in the other 5 portals | Add `npm test`/`npm audit --audit-level=high` to sohamyoga-frontend's `ci.yml`; add a minimal CI workflow to the other 5 portals | Open |
| SEC-03 | Dependency vulnerabilities | 3 of 5 Node portals (market-research-portal, voice-agent-platform, password-manager) share 5 identical high-severity `npm audit` findings (Next.js DoS/deserialization, postcss XSS/file-read, glob command injection via eslint-config-next) — none fixable without a major-version bump | **High** | market-research-portal, voice-agent-platform, password-manager | Continuous (dependency-tree exposure) | `npm audit --json` output, saved at `/tmp/.../scratchpad/audit_{market,voice,password}.json` this session | Coordinate a `next`/`eslint-config-next` major-version upgrade across the 3 portals; track as one remediation, not three | Open |
| SEC-04 | Dependency vulnerabilities | sohamyoga-frontend carries 39 `npm audit` findings (15 high, 5 moderate, 19 low) across 1,109 dependencies — largest raw count in the repo | **High** | sohamyoga-frontend | Continuous | `npm audit --json`, 25/39 have a non-breaking fix available | Run `npm audit fix` for the 25 non-breaking findings first (low-risk); scope the remaining 14 (major bump) separately | Open |
| SEC-05 | CSRF | Zero CSRF token/double-submit/Origin-check protection anywhere in the repo — all 6 portals rely solely on `SameSite=Lax` for CSRF mitigation on cookie-authenticated state-changing endpoints | **Medium** | All 6 | Low (SameSite=Lax is a real, modern-browser mitigation for the classic attack shape) | Repo-wide grep for `csrf` returned zero matches in every portal; confirmed via direct code read on 15+ state-changing handlers across portals | Add a CSRF token or custom-header requirement as defense-in-depth, prioritizing state-changing endpoints with real financial/business impact (checkout, campaign send, call placement) | Open |
| SEC-06 | Rate limiting | voice-agent-platform's real outbound phone-call placement (`POST /api/admin/calls/place`) and its login endpoint both have zero rate limiting — combined cost-abuse + credential-stuffing exposure on the same portal | **High** | voice-agent-platform | Low today (single-operator, not public-facing for calling) but rises sharply if ever exposed further | grep-confirmed zero rate-limiting code in the repo | Add a per-user/per-hour cap on call placement (cheap, high-value) before any wider exposure; add login throttling | Open |
| SEC-07 | Rate limiting | password-manager, ai-orchestrator-platform, and market-research-portal's login endpoints have no rate limiting or lockout | **Medium** | password-manager, ai-orchestrator-platform, market-research-portal | Medium (credential-stuffing exposure, partially offset by real per-attempt hashing cost — Argon2id/scrypt) | grep-confirmed absent in each; password-manager confirmed via direct code read of `login/route.ts` | Add a simple in-memory or DB-backed attempt counter with backoff | Open |
| SEC-08 | XSS | ai-orchestrator-platform's `DocxViewer.tsx` renders `mammoth.convertToHtml()` output via `dangerouslySetInnerHTML` with **zero sanitization** — no DOMPurify anywhere in the codebase or dependencies | **Medium** | ai-orchestrator-platform | Low-Medium (requires attacker to place a malicious `.docx` in a scoped, trusted project root first — single-operator threat model limits blast radius) | `DocxViewer.tsx:56`, confirmed `grep -rn "DOMPurify\|sanitize"` returns zero matches in `frontend/src/` | Wrap the mammoth output in DOMPurify before render — cheap, direct fix; sohamyoga-frontend's `SafeHtml.tsx`/`sanitize.ts` pattern is a ready-made template | Open |
| SEC-09 | Cookie security | ai-orchestrator-platform's session cookie sets `secure=False` unconditionally, even though the app is reachable over HTTPS via a public Cloudflare tunnel | **Medium** | ai-orchestrator-platform | Medium (session token exposed if any hop is ever plain HTTP; today the code comment claims the tunnel always terminates TLS before reaching the app, which is plausible but not independently verified in this pass) | `backend/app/main.py:117-120`, explicit `secure=False` with an inline comment | Set `secure=True` when `ORCH_AUTH_PASSWORD`/tunnel mode is active, or make it env-conditional like the Node portals already do | Open |
| SEC-10 | Cookie security | SohamYoga .NET's auth cookie uses `CookieSecurePolicy.SameAsRequest` (not hardcoded `Always`) — ships without `Secure` if the original request arrives over plain HTTP | **Low** | SohamYoga .NET | Low (behind nginx in the normal deployment path; only relevant if HTTP is ever reachable directly) | `Program.cs:55` | Set `CookieSecurePolicy.Always` if HTTP is never a legitimate access path | Open |
| SEC-11 | CSP | 3 of 6 portals (password-manager, voice-agent-platform, ai-orchestrator-platform) have **no CSP header at all**; the 3 that do (sohamyoga-frontend, market-research-portal, SohamYoga .NET) all allow `unsafe-inline`/`unsafe-eval` | **Low-Medium** | password-manager, voice-agent-platform, ai-orchestrator-platform (no CSP); all 6 (weak CSP where present) | Low standalone (defense-in-depth layer, not a standalone exploit) | Confirmed via `next.config.mjs`/`Program.cs` reads across all 6 portals | Add a baseline CSP to the 3 missing it; consider nonce-based CSP to drop `unsafe-inline` where feasible | Open |
| SEC-12 | Security headers | No portal sets `Strict-Transport-Security` explicitly (SohamYoga .NET relies on `UseHsts()` defaults in non-dev only; the other 5 set no HSTS at all) | **Low** | All 6 | Low (mitigated by tunnel/proxy-level TLS in most deployments) | grep-confirmed across all 6 | Add explicit HSTS headers where each portal terminates or is proxied to HTTPS | Open |
| SEC-13 | Secrets in default configs | Two integration docker-compose files (`integrations/medusa`, `integrations/offerkit`) hardcode default DB passwords (`medusa_pass`, `offerkit_pass`) directly in the compose file rather than via env var | **Low** | integrations/medusa, integrations/offerkit | Low (internal Docker network only, not internet-facing) | `git grep` output, this session | Move to env-var-driven passwords matching the `POSTGRES_PASSWORD: change-me-before-production` pattern already used elsewhere in the repo | Open |
| SEC-14 | Timing side-channel | voice-agent-platform's webhook/MCP shared-secret comparison uses plain `!==` instead of a constant-time compare | **Low** | voice-agent-platform | Very low (theoretical timing attack, high effort for low value on a shared-secret header) | `src/app/api/webhooks/vapi/route.ts:61-72`, `src/app/api/mcp/route.ts:19-25` | Switch to `crypto.timingSafeEqual` | Open |
| SEC-15 | RBAC/auth-gate coverage | **No gap found** — exhaustively verified: 213/213 `/api/admin/*` and 40/40 `/api/customer/*` route files in sohamyoga-frontend are gated (directly or via a shared handler) | N/A — clean | sohamyoga-frontend | N/A | This session's direct grep + manual verification of every apparent exception | 41 top-level `/api/*` directories outside admin/customer scope not yet individually re-verified — real, scoped, honestly-stated remaining gap, not claimed as covered | Scoped follow-up (P2) |
| SEC-16 | Secrets hygiene | **No gap found** — zero `.env*` files tracked in git (current tree or full history), zero private key blocks, zero AWS-style keys, zero hardcoded JWT/session secrets repo-wide | N/A — clean | All 6 | N/A | Commands run and logged this session (git grep + git log --all --diff-filter=A) | None needed — maintain this discipline | Clean |
| SEC-17 | Encryption design | **Positive finding** — password-manager's zero-knowledge crypto (client-side PBKDF2 600k + AES-256-GCM, server-side Argon2id re-hash) is genuinely sound, matching Bitwarden's documented defaults | N/A — positive | password-manager | N/A | Reality Matrix + this session's re-confirmation | Independent security review recommended before any real-world use (never run end-to-end, never audited) — see Reality Matrix | Needs live E2E test + audit before production use |

## Severity summary

| Severity | Count |
|---|---|
| High | 5 (SEC-01, 02, 03, 04, 06) |
| Medium | 5 (SEC-05, 07, 08, 09, 11) |
| Low | 4 (SEC-10, 12, 13, 14) |
| Clean/Positive | 3 (SEC-15, 16, 17) |

**Highest-value next actions** (feeds TOP_10_P0_P1_ACTIONS if/when that document is built): SEC-02
(CI security gates) and SEC-03/04 (dependency upgrades) are the two with the best
effort-to-risk-reduction ratio — both are mechanical, well-scoped, and close real, currently-exploitable
gaps rather than theoretical ones. SEC-01 (vault dev-mode) is the highest-severity single item but
requires an architecture decision (persistent vault vs. accepted risk), not just a config change.
