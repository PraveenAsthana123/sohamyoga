# C4 Model — sohamyoga-frontend

Filled from real `docker-compose.yml` topology and code reads — do not reuse the generic
`{{PLACEHOLDER}}` template at [../ARCHITECTURE.md](../ARCHITECTURE.md) for this portal; its
Redis/Celery/MLflow containers do not exist here.

## L1 — System Context

```mermaid
graph TB
    Customer([Customer / public visitor]):::actor
    Admin([Marketing / studio admin]):::actor

    System[sohamyoga-frontend<br/>Enterprise digital-marketing suite<br/>+ yoga-studio vertical]:::system

    Google([Google Business Profile API]):::external
    Postiz([Postiz — self-hosted social scheduler]):::external
    Skyvern([Skyvern — self-hosted browser automation]):::external
    Ollama([Ollama — local LLM]):::external
    GDrive([Google Drive API]):::external
    Slack([Slack API]):::external
    OpenBao([OpenBao — secrets vault]):::external

    Customer -->|HTTPS| System
    Admin -->|Authenticated HTTPS| System
    System -->|REST, OAuth| Google
    System -->|REST, self-hosted| Postiz
    System -->|REST, self-hosted| Skyvern
    System -->|Local HTTP| Ollama
    System -->|OAuth| GDrive
    System -->|Bearer token| Slack
    System -->|Fetch secrets at runtime| OpenBao

    classDef actor fill:#bef,stroke:#2c5282
    classDef external fill:#fef,stroke:#7c3aed
    classDef system fill:#1e40af,color:#fff,stroke:#1e3a8a,stroke-width:3px
```

## L2 — Containers (real, from `docker-compose.yml`)

```mermaid
graph TB
    subgraph "sohamyoga-frontend deployment"
        Nginx[nginx<br/>reverse proxy]
        FE[frontend<br/>Next.js 14 · port 3010<br/>221 pages, 383 API routes]
        BE[backend<br/>.NET SohamYoga.Web<br/>auth/session]
        PG[(postgres<br/>Postgres 16<br/>496 tables)]
        OB[ollama-bridge]
        OHB[ollama-host-bridge]
        Cron[cron<br/>node-cron / tsx runner]
    end

    Customer([Customer]):::actor
    Admin([Admin]):::actor

    Customer -->|HTTPS| Nginx
    Admin -->|HTTPS| Nginx
    Nginx --> FE
    FE -->|"/api/auth/me"| BE
    FE -->|raw pg, no ORM| PG
    FE -->|local LLM calls| OB
    OB --> OHB
    BE --> PG
    Cron -->|scheduled jobs, e.g. SecurityScanJob| PG

    classDef actor fill:#bef,stroke:#2c5282
```

No Redis, no message broker, no Celery/MLflow — this system is simpler than a typical
FastAPI+Celery+Redis+MLflow stack; verify before assuming those components exist.

## L3 — Component view (frontend container, partial — 44 API subfolders is too many to diagram flat)

Representative slice, not exhaustive:

```mermaid
graph TB
    subgraph "frontend (Next.js)"
        Pages[App Router pages<br/>221 total: 142 admin / 32 customer / ~47 public]
        API[API routes<br/>383 route.ts across 44 subfolders]
        AdminAuth[admin-auth.ts<br/>requireAdmin gate]
        CustAuth[customer-auth.ts<br/>requireCustomer gate]
        Domain[src/domain/*<br/>57 domain modules]
        PGLib[postgres.ts<br/>raw pg pool]
        Scanners[security/scanners/<br/>semgrep · ZAP · npm audit · trivy/checkov]
    end

    Pages --> API
    API --> AdminAuth
    API --> CustAuth
    API --> Domain
    Domain --> PGLib
    Domain --> Scanners
```

## Architectural decisions

See [ADR/](ADR/): 0001 (no ORM), 0002 (self-hosted web push), 0003 (explicit UI-coverage flags),
0004 (security scans in-app not CI).
