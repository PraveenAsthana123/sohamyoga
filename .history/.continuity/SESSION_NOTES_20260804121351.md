# Session Notes

## Goals
- Strengthen the local Ollama integration in the frontend so the assistant exposes health status and degrades gracefully when the model backend is unavailable.

## Blockers
- None.

## Key Decisions
- The assistant now uses the existing `/api/ai/health` probe and the chat endpoint will return a clear 503 when Ollama is unavailable.
