'use client';import{FormEvent,useCallback,useEffect,useState}from'react';type R=Record<string,any>;const empty={agents:[]as R[],scripts:[]as R[],calls:[]as R[],events:[]as R[],audio:[]as R[],trunks:[]as R[],endpoints:[]as R[],workflows:[]as R[],routes:[]as R[],profiles:[]as R[],connection:null as R|null,monitor:null as R|null,summary:{} as R};
export default function VoiceAI(){const[d,setD]=useState(empty),[msg,setMsg]=useState(''),[agent,setAgent]=useState({name:'Marketing qualification assistant',purpose:'Qualify consented leads and book appointments',language:'English',voiceName:'en',instructions:'Be concise, disclose AI use, never make medical or pricing promises.',knowledgeScope:'Approved studio services and booking availability only.'}),[script,setScript]=useState({name:'Consented lead follow-up',scriptType:'follow_up',opening:'Hello, this is the SohamYoga virtual assistant following up on your request. This call may be recorded and processed by AI. Is now a good time?',cta:'Would you like me to help schedule an introductory session?',disclosures:'Identify the business, AI assistant, purpose and recording before qualification.',forbiddenClaims:'No medical outcomes, guaranteed results, invented prices or unavailable appointments.'}),[preview,setPreview]=useState('Hello, this is the SohamYoga virtual assistant. This is a local voice preview and no telephone call is being placed.'),[phone,setPhone]=useState('+14035550100');const load=useCallback(()=>fetch('/api/voice-ai',{cache:'no-store'}).then(r=>r.json()).then(setD),[]);useEffect(()=>{void load()},[load]);async function act(body:R){const r=await fetch('/api/voice-ai',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),j=await r.json();setMsg(r.ok?'Saved':j.error);await load();return j}async function createAgent(e:FormEvent){e.preventDefault();await act({action:'create_agent',...agent})}async function createScript(e:FormEvent){e.preventDefault();await act({action:'create_script',...script,agentId:d.agents[0]?.id})}async function voice(e:FormEvent){e.preventDefault();const r=await fetch('/api/voice-ai/preview',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title:'Voice AI compliance preview',text:preview,voiceName:'en'})}),j=await r.json();setMsg(r.ok?`Voice preview ready: ${j.asset.filePath}`:j.error);await load()}
return <div className="space-y-6"><header><h1 className="text-2xl font-bold">Digital Marketing Voice AI</h1><p className="text-sm text-gray-500">Inbound and outbound operations: SIP, scripts, workflows, voice profiles, consent, calls, responses and evidence.</p></header>{msg&&<p role="status" className="rounded border bg-blue-50 p-2 text-sm">{msg}</p>}<section className="grid gap-3 md:grid-cols-5">{[['PSTN',d.connection?.status||'missing'],['PBX',d.monitor?.healthy?'healthy':'not reported'],['Calls',d.summary?.total||0],['Completed',d.summary?.completed||0],['Total duration',`${d.summary?.duration_seconds||0}s`]].map(([k,v])=><div key={String(k)} className="rounded-xl border bg-white p-3"><div className="text-xs text-gray-500">{k}</div><strong>{v}</strong></div>)}</section><section className="rounded-xl border bg-white p-4"><div className="flex justify-between"><strong>PSTN readiness</strong><span className={d.connection?.status==='connected'?'text-green-700':'text-amber-700'}>{d.connection?.status||'missing'}</span></div><p className="text-sm text-gray-500">{d.connection?.provider||'No telephony provider'} — outbound jobs stay blocked until a trunk is registered and the provider is connected.</p></section>
<section className="grid gap-5 lg:grid-cols-2"><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">SIP / PJSIP control plane</h2><p className="my-2 text-sm text-gray-500">Secrets stay in environment or Vault; this UI stores references only. New routes are disabled by default.</p><button onClick={()=>act({action:'create_trunk',name:`Local Asterisk ${d.trunks.length+1}`,provider:'Asterisk/PJSIP',host:'127.0.0.1',port:15060,transport:'udp',authUsername:'1001',secretRef:'ASTERISK_SIP_SECRET'})} className="rounded bg-slate-800 px-3 py-2 text-white">Add local trunk record</button>{d.trunks.map(t=><div key={t.id} className="mt-3 rounded border p-2 text-sm"><strong>{t.name}</strong> · {t.status}<div className="text-gray-500">{t.host}:{t.port}/{t.transport} · secret: {t.secret_ref||'not assigned'}</div></div>)}</div><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Workflows and inbound routing</h2><button onClick={()=>act({action:'create_workflow',name:`Qualification workflow ${d.workflows.length+1}`,direction:'both',steps:[{type:'disclosure'},{type:'consent_check'},{type:'qualify'},{type:'appointment'},{type:'human_handoff'}]})} className="mt-2 rounded bg-indigo-700 px-3 py-2 text-white">Create safe workflow</button>{d.workflows.map(w=><div key={w.id} className="mt-3 rounded border p-2 text-sm"><strong>{w.name}</strong> · {w.direction} · {w.status}<div>{Array.isArray(w.steps)?w.steps.map((s:R)=>s.type).join(' → '):''}</div>{!d.routes.some(r=>r.workflow_id===w.id)&&<button onClick={()=>act({action:'create_route',name:`Inbound route ${d.routes.length+1}`,workflowId:w.id,agentId:d.agents[0]?.id,scriptId:d.scripts.find(s=>['approved','active'].includes(s.status))?.id,fallbackExtension:'1001'})} className="mt-2 rounded border px-2 py-1">Add disabled inbound route</button>}</div>)}</div></section>
<section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Voice modulation profiles</h2><p className="text-sm text-gray-500">Rate, pitch and volume are bounded. Voice cloning requires a recorded consent reference.</p><button onClick={()=>act({action:'create_profile',name:`Professional profile ${d.profiles.length+1}`,voiceName:'en',language:'English',speakingRate:1,pitch:0,volume:0,style:'professional'})} className="mt-2 rounded bg-violet-700 px-3 py-2 text-white">Create profile</button><div className="mt-3 flex flex-wrap gap-2">{d.profiles.map(p=><span key={p.id} className="rounded border px-3 py-2 text-sm">{p.name}: {p.speaking_rate}×, {p.pitch_semitones} st, {p.status}</span>)}</div></section>
<section className="grid gap-5 lg:grid-cols-2"><form onSubmit={createAgent} className="space-y-2 rounded-xl border bg-white p-5"><h2 className="font-semibold">Create Voice AI agent</h2>{Object.entries(agent).map(([k,v])=><label key={k} className="block text-sm">{k}<input className="mt-1 w-full rounded border p-2" value={v} onChange={e=>setAgent({...agent,[k]:e.target.value})}/></label>)}<button className="rounded bg-brand-600 px-4 py-2 text-white">Create draft agent</button></form><form onSubmit={createScript} className="space-y-2 rounded-xl border bg-white p-5"><h2 className="font-semibold">Create versioned call script</h2>{Object.entries(script).map(([k,v])=><label key={k} className="block text-sm">{k}<textarea className="mt-1 w-full rounded border p-2" value={v} onChange={e=>setScript({...script,[k]:e.target.value})}/></label>)}<button className="rounded bg-gray-800 px-4 py-2 text-white">Create draft script</button></form></section>
<form onSubmit={voice} className="space-y-2 rounded-xl border bg-white p-5"><h2 className="font-semibold">Generate real local voice preview</h2><textarea className="h-24 w-full rounded border p-2" value={preview} onChange={e=>setPreview(e.target.value)}/><button className="rounded bg-indigo-700 px-4 py-2 text-white">Generate WAV</button><div className="flex flex-wrap gap-3">{d.audio.map(a=><audio key={a.id} controls src={a.file_path}/>)}</div></form>
<section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Scripts and outbound scheduling</h2><label className="mt-3 block text-sm">Test E.164 phone<input className="ml-2 rounded border p-1" value={phone} onChange={e=>setPhone(e.target.value)}/></label>{d.scripts.map(s=><div key={s.id} className="mt-3 rounded border p-3 text-sm"><div className="flex justify-between"><strong>{s.name} v{s.version}</strong><span>{s.status}</span></div><p className="my-2 text-gray-600">{s.opening}</p><div className="flex gap-2">{['draft','review'].includes(s.status)&&<button onClick={()=>act({action:'approve_script',scriptId:s.id})} className="rounded bg-green-700 px-3 py-1 text-white">Approve</button>}{['approved','active'].includes(s.status)&&<button onClick={()=>act({action:'queue_call',scriptId:s.id,agentId:s.agent_id,phone,consentBasis:'Explicit request for follow-up',dncChecked:true,scheduledAt:new Date(Date.now()+3600000).toISOString()})} className="rounded bg-red-700 px-3 py-1 text-white">Schedule consented test +1h</button>}</div></div>)}</section>
<section className="grid gap-5 lg:grid-cols-2"><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Call monitoring</h2>{d.calls.map(c=><div key={c.id} className="mt-2 rounded border p-2 text-xs"><strong>{c.direction}</strong> · {c.status} · {c.phone_e164||'unknown'}{c.blocker&&<div className="text-amber-700">{c.blocker}</div>}</div>)}{!d.calls.length&&<p className="mt-2 text-sm text-gray-400">No calls.</p>}</div><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Provider/event log</h2>{d.events.map(e=><div key={e.id} className="mt-2 text-xs">{new Date(e.occurred_at).toLocaleString()} · {e.event_name}</div>)}{!d.events.length&&<p className="mt-2 text-sm text-gray-400">No call events.</p>}</div></section></div>}
